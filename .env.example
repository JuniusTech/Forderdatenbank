# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_user
DB_PASSWORD=your_password
DB_NAME=culinary_funding

# API (Faz 4)
PORT=3009

# Alternatif: tek satır connection string (DB_* yerine geçer)
# DATABASE_URL=postgresql+psycopg://user:pass@host:5432/culinary_funding

# Crawler
CRAWL_DELAY_SECONDS=30
BASE_URL=https://www.foerderdatenbank.de

# Yerel Docker (dev)
# POSTGRES_USER=culinary
# POSTGRES_PASSWORD=culinary_dev
# POSTGRES_DB=culinary_funding
# POSTGRES_PORT=15432
